/** Wide navy-and-gold city vista that matches WeekendGate chrome. */
export const HOTEL_SEARCH_DEFAULT_BACKDROP = "/media/hero/hotel-search-navy-gold.jpg?v=3";

/** Background for the hotel results search engine. */
export function hotelSearchBackdrop(_destination?: string): string {
  return HOTEL_SEARCH_DEFAULT_BACKDROP;
}
